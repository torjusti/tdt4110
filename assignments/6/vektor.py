# Ikke ferdig

def vec(a, b, c): return [a, b, c]

def vec_print(vec): print(vec) # fuck u

def vec_multiply_scalar(vec, scalar):  reduce(lambda component, scalar: component * scalar, vec)

print(vec_multiply_scalar())

sum(component * vec2[i] for component, i in vec1)

u*v = u1*v1 + v2*u2 + v3*u3
